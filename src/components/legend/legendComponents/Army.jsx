import * as React from "react";

const Army = (props) => (
  <svg height={14} width={23} {...props}>
    <circle 
        cx={10} cy={7} r={6} 
        stroke="#404040" 
        strokeWidth={2} 
        fill="#737373" 
    />
  </svg>
)

export default Army;